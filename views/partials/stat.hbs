<div class="stat rv">
  <div class="lab">{{label}}</div>
  <div class="num {{#unless countTo}}txt{{/unless}}">
    {{#if countTo}}<span class="count" data-to="{{countTo}}">0</span>{{else}}{{value}}{{/if}}<span class="u">{{unit}}</span>
  </div>
</div>
